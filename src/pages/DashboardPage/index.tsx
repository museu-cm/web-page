import { Stack, Typography } from "@mui/material";

const DashboardPage = () => {
  return(
    <Stack display="flex" alignContent="flex-start">
      <Typography variant="h3">Dashboard</Typography>
    </Stack>
  );
};

export default DashboardPage;